version '0.0.1'

subprojects {
    apply plugin: 'java'

    group "com.github.tth05.teth"

    repositories {
        mavenCentral()
    }

    compileJava {
        options.compilerArgs << '--enable-preview'
    }

    compileTestJava {
        options.compilerArgs << '--enable-preview'
    }
}
